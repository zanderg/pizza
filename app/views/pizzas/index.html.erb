<h1>Listing pizzas</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Sauce</th>
      <th>Toppings</th>
      <th>Time to cook</th>
      <th>Cost</th>
      <th>Date created</th>
      <th>Time put in</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pizzas.each do |pizza| %>
      <tr>
        <td><%= pizza.name %></td>
        <td><%= pizza.sauce %></td>
        <td><%= pizza.toppings %></td>
        <td><%= pizza.time_to_cook %></td>
        <td><%= pizza.cost %></td>
        <td><%= pizza.date_created %></td>
        <td><%= pizza.time_put_in %></td>
        <td><%= link_to 'Show', pizza %></td>
        <td><%= link_to 'Edit', edit_pizza_path(pizza) %></td>
        <td><%= link_to 'Destroy', pizza, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pizza', new_pizza_path %>
